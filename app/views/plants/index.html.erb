<h1>Your Plants</h1>

<ul>
  <% @plants.each do |plant| %>
    <li>
      <% if plant.images.attached? %>
      <% end%>
    </li>
    <li>
      <%= plant.name %>
      is born on <%= plant.birthday %> and
      was last waterd at <%= plant.last_watering %>
    </li>
    <li>
        Check out more here: <%= link_to plant.name, plant %> 
    </li>
  <% end %>
</ul>

<%= link_to "New Plant", new_plant_path %>
